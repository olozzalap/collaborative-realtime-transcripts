{
  "name": "crt-backend",
  "version": "0.1.0",
  "description": "Collaborative Realtime Transcripts - Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js && cd ../frontend && npm install && npm build",
    "server": "nodemon server.js",
    "dev": "node server.js && nodemon server.js && cd ../frontend && npm install && npm start",
    "heroku-postbuild": "cd ../frontend && npm install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/olozzalap/collaborative-realtime-transcripts.git"
  },
  "author": "Eben Palazzolo",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/olozzalap/collaborative-realtime-transcripts/issues"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "dotenv": "^6.2.0",
    "event-stream": "3.3.4",
    "express": "^4.16.3",
    "mongoose": "^5.2.14",
    "socket.io": "^2.1.1",
    "socket.io-client": "^2.1.1"
  },
  "devDependencies": {
    "nodemon": "^1.18.4"
  }
}
